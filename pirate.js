(()=>{const c=document.getElementById('sea'),ctx=c.getContext('2d');const islands=[{x:140,y:110,n:'Whisper Key',type:'trade'},{x:360,y:420,n:'Ember Isle',type:'crew'},{x:590,y:150,n:'Moonstone Atoll',type:'treasure'},{x:820,y:360,n:'Stormwatch',type:'repair'},{x:730,y:90,n:'Driftwood Cay',type:'trade'},{x:300,y:170,n:'Coral Reach',type:'crew'}];let ship,gold,crew,hull,cargo,rep,found,weather,visited;const names=['Mira — navigator','Kian — carpenter','Sana — quartermaster','Dev — lookout','Ravi — deckhand','Ira — cook','Noah — sailor','Zoya — cartographer'];function reset(){ship={x:500,y:290};gold=150;crew=8;hull=100;cargo=0;rep=10;found=0;weather='Calm seas';visited=new Set();crewList();event('The crew raises the sails. Your first island is waiting.');draw();hud();}function hud(){document.getElementById('gold').textContent=gold;document.getElementById('crew').textContent=crew;document.getElementById('hull').textContent=hull;document.getElementById('cargo').textContent=`${cargo} / 24`;document.getElementById('rep').textContent=rep;document.getElementById('weather').textContent=weather;}function event(s){document.getElementById('event').innerHTML=s;}function crewList(){document.getElementById('crewList').innerHTML=names.slice(0,Math.min(crew,names.length)).map((n,i)=>`<div><strong>${n.split(' — ')[0]}</strong><span>${n.split(' — ')[1]} • ${i%2?'steady':'ready'}</span></div>`).join('');}function draw(){ctx.fillStyle='#07131f';ctx.fillRect(0,0,c.width,c.height);for(let i=0;i<90;i++){ctx.fillStyle='rgba(255,255,255,.06)';ctx.beginPath();ctx.arc((i*83)%c.width,(i*47)%c.height,1.4,0,Math.PI*2);ctx.fill()}islands.forEach((is,i)=>{const seen=visited.has(i);ctx.fillStyle=seen?'#3b6f58':'#516b53';ctx.beginPath();ctx.ellipse(is.x,is.y,45,27,Math.sin(i),0,Math.PI*2);ctx.fill();ctx.fillStyle='#e7d6a6';ctx.font='13px Inter';ctx.textAlign='center';ctx.fillText(seen?is.n:'???',is.x,is.y+50);});ctx.save();ctx.translate(ship.x,ship.y);ctx.rotate(Math.sin(ship.x/40)*.08);ctx.fillStyle='#8c5b3e';ctx.beginPath();ctx.moveTo(0,-18);ctx.lineTo(15,18);ctx.lineTo(-15,18);ctx.closePath();ctx.fill();ctx.fillStyle='#e6e1d5';ctx.fillRect(-3,-30,6,30);ctx.restore();}function move(dx,dy){ship.x=Math.max(30,Math.min(c.width-30,ship.x+dx));ship.y=Math.max(30,Math.min(c.height-30,ship.y+dy));hull=Math.max(0,hull-(Math.random()<.06?1:0));if(Math.random()<.04){weather=['Calm seas','Crosswind','Fog bank','Strong current'][Math.floor(Math.random()*4)];event(`The weather shifts: <strong>${weather}</strong>. The crew adjusts the sails.`)}islands.forEach((is,i)=>{if(Math.hypot(ship.x-is.x,ship.y-is.y)<55&&!visited.has(i)){visited.add(i);found++;discover(is,i)}});hud();draw();}function discover(is,i){const outcomes={trade:[['Spices found in a hidden cove.',40,5],['A local trader offers a fair deal.',25,3]],crew:[['A stranded sailor asks to join your crew.',-10,2],['Your crew finds a useful supply cache.',30,4]],treasure:[['A chart points to a valuable wreck nearby.',70,7],['You find a small chest beneath the tide line.',90,6]],repair:[['An old dock lets your carpenter restore the hull.',0,10],['The island sells rare repair materials.',-20,15]]};const o=outcomes[is.type][Math.floor(Math.random()*outcomes[is.type].length)];gold=Math.max(0,gold+o[1]);if(is.type==='crew')crew=Math.min(8,crew+o[2]);else hull=Math.min(100,hull+o[2]);rep+=2;event(`<strong>${is.n}</strong><br>${o[0]}<br><small>Gold ${o[1]>=0?'+':''}${o[1]} • Reputation +2</small>`);crewList();}function explore(){if(found===0)return event('Sail toward an island first. The fog is hiding something.');gold=Math.max(0,gold-10);rep+=3;event('The crew explores the shoreline and updates the chart. You gain reputation for helping the locals.');hud();}function trade(){if(cargo===0){cargo=6;event('The quartermaster loads 6 units of spices. Cargo is now moving through your trade route.')}else{gold+=cargo*8;event(`Cargo sold for ${cargo*8} gold. The crew celebrates a profitable run.`);cargo=0}rep+=1;hud();}function repair(){const cost=25;if(hull>=100)return event('The hull is already in good shape.');if(gold<cost)return event('Not enough gold for repairs.');gold-=cost;hull=Math.min(100,hull+25);event('The carpenter patches the hull while the crew keeps watch.');hud();}function crewAction(){if(crew<8){crew++;event('A new sailor signs on after hearing about your voyage.');}else{gold+=15;event('The crew works together to unload supplies. +15 gold.')}hud();crewList();}document.querySelectorAll('[data-dir]').forEach(b=>b.onclick=()=>{const d=b.dataset.dir;move(d==='left'?-24:d==='right'?24:0,d==='up'?-24:d==='down'?24:0)});window.addEventListener('keydown',e=>{if(['ArrowUp','w'].includes(e.key))move(0,-24);if(['ArrowDown','s'].includes(e.key))move(0,24);if(['ArrowLeft','a'].includes(e.key))move(-24,0);if(['ArrowRight','d'].includes(e.key))move(24,0)});document.getElementById('reset').onclick=reset;document.getElementById('explore').onclick=explore;document.getElementById('trade').onclick=trade;document.getElementById('repair').onclick=repair;document.getElementById('crewAction').onclick=crewAction;reset();})();
