(()=>{const c=document.getElementById('map'),x=c.getContext('2d');let W,H,D=devicePixelRatio||1,run=false,paused=false,last=0,score=0,combo=0,energy=100,wave=1,targets=[],particles=[];function fit(){W=c.clientWidth;H=c.clientHeight;c.width=W*D;c.height=H*D;x.setTransform(D,0,0,D,0,0)}addEventListener('resize',fit);fit();function spawn(){targets=[];const n=Math.min(3+wave,9);for(let i=0;i<n;i++)targets.push({x:50+Math.random()*(W-100),y:80+Math.random()*(H-190),r:16+Math.random()*16,vx:(Math.random()-.5)*(1+wave*.15),vy:(Math.random()-.5)*(1+wave*.15),life:3+Math.random()*3})}function burst(px,py){for(let i=0;i<16;i++)particles.push({x:px,y:py,vx:(Math.random()-.5)*4,vy:(Math.random()-.5)*4,a:1})}function draw(t){x.clearRect(0,0,W,H);x.fillStyle='#030913';x.fillRect(0,0,W,H);for(let i=0;i<90;i++){x.fillStyle='rgba(60,160,220,.08)';x.fillRect((i*83)%W,(i*47)%H,2,2)}targets.forEach(q=>{x.beginPath();x.arc(q.x,q.y,q.r,0,7);x.fillStyle='rgba(34,211,238,.12)';x.fill();x.beginPath();x.arc(q.x,q.y,q.r*.55,0,7);x.strokeStyle='#22d3ee';x.lineWidth=2;x.stroke();x.beginPath();x.arc(q.x,q.y,q.r*.16,0,7);x.fillStyle='#fff';x.fill()});particles.forEach(p=>{x.globalAlpha=p.a;x.fillStyle='#22d3ee';x.fillRect(p.x,p.y,3,3);x.globalAlpha=1});if(run&&!paused){const dt=Math.min(.05,(t-last)/1000||0);last=t;targets.forEach(q=>{q.x+=q.vx*60*dt;q.y+=q.vy*60*dt;q.life-=dt;if(q.x<q.r||q.x>W-q.r)q.vx*=-1;if(q.y<q.r+55||q.y>H-q.r-70)q.vy*=-1});particles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.a-=.025});particles=particles.filter(p=>p.a>0);if(!targets.length||targets.every(q=>q.life<=0)){wave++;energy=Math.max(0,energy-5);spawn()}ui()}requestAnimationFrame(draw)}requestAnimationFrame(draw);function recordBest(){try{const v=Math.floor(score),old=Number(localStorage.getItem('funhub_best_precision')||0);if(v>old)localStorage.setItem('funhub_best_precision',v)}catch{}}function ui(){recordBest();score=Math.max(0,score);document.getElementById('score').textContent=Math.floor(score);document.getElementById('combo').textContent=combo;document.getElementById('energy').textContent=Math.floor(energy);document.getElementById('wave').textContent=wave}c.addEventListener('pointerdown',e=>{if(!run||paused)return;const r=c.getBoundingClientRect(),px=e.clientX-r.left,py=e.clientY-r.top;let hit=-1,best=999;targets.forEach((q,i)=>{const d=Math.hypot(px-q.x,py-q.y);if(d<q.r&&d<best){best=d;hit=i}});if(hit>=0){const q=targets[hit],ratio=best/q.r;const pts=Math.round(80+140*(1-ratio)+combo*8);score+=pts;combo++;burst(q.x,q.y);targets.splice(hit,1);document.getElementById('info').textContent='Calibration hit: +'+pts+' points. Keep the combo alive.'}else{combo=0;energy=Math.max(0,energy-3);document.getElementById('info').textContent='Missed calibration node — combo reset.'}ui()});document.getElementById('start').onclick=()=>{run=true;paused=false;score=0;combo=0;energy=100;wave=1;spawn();toast('Simulation online')};document.getElementById('pause').onclick=()=>{paused=!paused;document.getElementById('pause').textContent=paused?'Resume':'Pause'};document.getElementById('focus').onclick=()=>{if(energy>=20){energy-=20;targets.forEach(q=>q.vx*=.45,q.vy*=.45);toast('Focus Pulse: motion slowed')}else toast('Not enough energy');ui()};document.getElementById('reset').onclick=()=>{run=false;paused=false;score=0;combo=0;energy=100;wave=1;targets=[];ui()};document.getElementById('fs').onclick=async()=>{try{if(!document.fullscreenElement)await document.documentElement.requestFullscreen();else await document.exitFullscreen()}catch{toast('Use browser menu for full-screen')}};function toast(s){const e=document.getElementById('toast');e.textContent=s;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1200)}ui()})();
